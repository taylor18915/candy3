@page
@model Candy3.Pages.Ord.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Orderscs[0].OName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Orderscs) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
